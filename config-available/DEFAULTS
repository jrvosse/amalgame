config('020-ag_prefixes', link).
config('025-amalgame', link).
config(ag_data, link).
config(ag_lod, link).
config(ag_menu, link).
config(ag_preferences, link).
config(ag_skin, link).
config(opm_graphviz, link).
