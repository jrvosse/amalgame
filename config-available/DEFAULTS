config('025-amalgame', link).
config(ag_data, link).
config(ag_lod, link).
config(ag_menu, link).
config(ag_preferences, link).
config(ag_modules, link).
config(ag_skin, link).
config(ag_strat_prov_graph_viz, link).
